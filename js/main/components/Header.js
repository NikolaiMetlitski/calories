'use strict';

var React = require('react');

/**
*
* Main menu.
*
*/
var Header = React.createClass({
  displayName: 'Header',

  render: function() {
    return (
      <header></header>
    );
  }
});

module.exports = Header;
